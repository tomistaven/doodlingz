/// All drawing tools available in the editor.
///
/// Order matches the intended tool panel layout — primary tools first,
/// shape tools grouped, extras at the end.
enum DrawingTool {
  brush,
  highlighter,
  spray,
  eraser,
  fill,
  line,
  rectangle,
  ellipse,
  triangle,
}